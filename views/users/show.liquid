{% layout "layout.liquid" %} {% block content %}
    <div>
        <article>
           User page
           
            {% comment %} <h2>
                {{ user.name }} -
                
                {% comment %} {% if readyToEat == true %}
                Ready to Eat
                {% else %}
                Not Ready to Eat
                {% endif %} {% endcomment %}
            </h2>
        
            <a href="/users/{{ user._id }}/edit"><button>Edit</button></a>
               
         <a href="/users/"><button>Back to Main Button</button></a> 
        </article>
    </div>
{% endblock %}
