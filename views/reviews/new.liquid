{% layout "layout.liquid" %} {% block content %}
    <div>
        {% comment %} <a href="/reviews/new"><button>Create A New Fruit</button></a> {% endcomment %}
        <form action='/reviews/' method="post">
          <h1 class="fw-bolder mb-1">Create a Review</h1>
          <div class="review-textbox">
            {% comment %} use onclick - can only use css and html - cannot do dom manipulation {% endcomment %}
            {% comment %} <div class="stars" data-submit-url="review/rate=true"> 
          </div>{% endcomment %}
            <h6>What did you think?</h6>
            Your rating:
              <input type="number" name="rating" id="rating" placeholder="1-5">
              <input type="hidden" name="bookId" value={{book._id}}>
                  <textarea name="reviewBody" class="textBox largeTextBox" id="" cols="30" rows="12" style="display: block; height: auto" placeholder="Enter your review here.">
                  </textarea>
                <input type="submit" value="Submit Review" class="btn btn-primary">
              </div>
        </form>
        {% comment %} {% for review in reviews %}
            <article>
                <a href="/reviews/{{ review._id }}">
                
                    Has Read: <input type="checkbox" name="readyToEat"><br>
                    </form>
                </a>
            </article>
        {% endfor %} {% endcomment %}

        {% comment %} maybe try to use this logic (ejs) to 
        <form id="add-per-to-cast" action="/movies/<%= movie._id%>/performers" method="POST">
        <select name="performerId">
          <%- performers.map(p => 
            `<option value="${p._id}">${p.name}</option>`
          ).join('') %>
        </select>
        <button type="submit">Add to Cast</button>
      </form> {% endcomment %}



    </div>
{% endblock %}
